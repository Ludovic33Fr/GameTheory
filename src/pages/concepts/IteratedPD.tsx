import { Layout } from '../../components/Layout';

export default function IteratedPDPage() {
  return (
    <Layout crumbs={[{ label: 'Concepts', to: '/' }, { label: 'Dilemme du prisonnier itéré' }]}>
      <div style={{ padding: 'var(--space-12) var(--space-16)' }}>
        <h1>Placeholder — Dilemme du prisonnier itéré</h1>
      </div>
    </Layout>
  );
}
